module.exports = (db) => {
        // db.aircraft.hasMany(db.flight, {foreignKey: 'aircraft',constraints:false});
        // db.aircraft.belongsTo(db.airline,{foreignKey: 'two_letter_code',constraints:false})
}