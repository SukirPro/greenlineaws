module.exports = (db) => {
        // db.passenger.belongsTo(db.flight, {foreignKey: 'flight_number',constraints:false});
    // db.threat.belongsTo(db.passenger);
        // db.passenger.hasOne(db.threat, {foreignKey: 'passport_number',constraints:false});
}