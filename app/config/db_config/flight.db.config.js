module.exports = (db) => {
    // db.flight.belongsTo(db.aircraft)
    // db.flight.belongsToMany(db.airport, { through: 'departure', foreignKey: 'iata_code', otherKey: 'flight_number', onDelete: 'cascade',constraints:false });
    // db.flight.belongsToMany(db.airport, {  through: 'arrival', foreignKey: 'iata_code', otherKey: 'flight_number', onDelete: 'cascade',constraints:false });
        // db.flight.hasMany(db.passenger, {as:'passenger',foreignKey: 'flight_number',otherKey:'flight_number',constraints:false});
}