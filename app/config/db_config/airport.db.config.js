module.exports = (db) => {   
        // db.airport.belongsToMany(db.flight, {  through: 'departure', foreignKey: 'iata_code', otherKey: 'flight_number', onDelete: 'cascade',constraints:false });
        // db.airport.belongsToMany(db.flight, { through: 'arrival', foreignKey: 'iata_code', otherKey: 'flight_number', onDelete: 'cascade',constraints:false });
}